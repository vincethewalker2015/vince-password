<header class="navbar navbar-fixed-top navbar-inverse">
    <div class="container">
      <nav>
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button> 
        <div class="collapse navbar-collapse" id="myNavbar">
            <ul class="nav navbar-nav navbar-left">
                <li><%= link_to "Home", root_path %></li>
            </ul>
            <ul class="nav navbar-nav navbar-right">
                <li><%#= link_to "Help", help_path %></li>
                <li><%= link_to "Index", passwords_path %></li>
                <% if current_user && current_user.admin? %>
                  <li><%= link_to "Create a Password", new_password_path %></li>
                <% end %>
                <% if current_user %>
                  <li><%= link_to "Log out", destroy_user_session_path, class: "btn btn-danger nav-but-red", method: :delete %></li>
                    <li></li>
                <% else %>
                  <li><%= link_to "Log in", new_user_session_path, class: "btn btn-primary nav-but-blue", method: :get %></li>
                <% end %>
            </ul>
        </div>
      </nav>
    </div>
</header>