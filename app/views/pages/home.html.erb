<div class="container">
    <div class="center jumbotron">
        <h1 align="center">Password App</h1>
    </div>
    <% if current_user && current_user.admin?%>
        <div align="center">
            <div class="home-buttons">
                <p><%= link_to "Create User", new_user_registration_path, class: "btn btn-success btn-lg" %></p>
            </div>
        </div>
    <% end %>

</div>